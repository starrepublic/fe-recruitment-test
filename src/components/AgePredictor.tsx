import "./AgePredictor.css";

const AgePredictor = () => {
  return (
    <div className="age-predictor">
      <h2>Age predictor</h2>

      <p>Type in your name and see what's your predicted age is.</p>

      <input type="" />
      <button onClick={() => alert("Not implemented")}>Check your age</button>
    </div>
  );
};

export default AgePredictor;
